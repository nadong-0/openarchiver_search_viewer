[project]
name = "openarchiver_search_viewer"
version = "0.1.0"
description = "Local OpenArchiver email search web app"
requires-python = ">=3.11,<3.13"
dependencies = [
    "fastapi>=0.115.0",
    "python-dotenv>=1.0.1",
    "requests>=2.32.3",
    "uvicorn[standard]>=0.30.0",
]

[tool.uv]
package = false
